{{- define "bandstand-web-service.tests.pod" -}}
{{- $relName := .Release.Name -}}
{{- if .Values.nameSuffix }}
{{- $relName = print .Release.Name "-" .Values.nameSuffix }}
{{- end }}
apiVersion: v1
kind: Pod
metadata:
  name: {{ $relName }}-acceptance-tests
  labels:
    application: {{ $relName }}-acceptance-tests
    application-under-test: {{ $relName }}
    version: {{ .Values.image.tag }}
    environment: {{ .Values.env }}
    owner: {{ .Values.owner }}
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-delete-policy": before-hook-creation
    "polaris.fairwinds.com/readinessProbeMissing-exempt": "true"
    "polaris.fairwinds.com/livenessProbeMissing-exempt": "true"
    "linkerd.io/inject": disabled
spec:
  containers:
    - name: {{ $relName }}-acceptance-tests
      image: ktechartifacts-docker.jfrog.io/{{ $relName }}-acceptance-tests:{{ .Values.image.tag }}
      imagePullPolicy: Always
      {{- $testContainer := .Values.testContainer | default dict }}
      {{- if $testContainer.command }}
      command: {{ $testContainer.command }}
      {{- end }}
      {{- if $testContainer.arguments }}
      args:
      {{- range $testContainer.arguments }}
      - {{ . }}
      {{- end }}
      {{- end }}
      resources:
        requests:
          cpu: 250m
          memory: 250Mi
        limits:
          cpu: 500m
          memory: 750Mi
      env:
        - name: ENV
          value: {{ .Values.env }}
      volumeMounts:
        - name: tmp
          mountPath: /tmp
          readOnly: false
        - name: tmp
          mountPath: /tmp/workspace # explicitly add workspace dir for backwards compatibility
          subPath: workspace
          readOnly: false
      securityContext:
        runAsNonRoot: true
        allowPrivilegeEscalation: false
        readOnlyRootFilesystem: true
        capabilities:
          drop:
            - all
  imagePullSecrets:
    - name: rt-docker-config
  volumes:
    - name: tmp
      emptyDir: { }
  securityContext:
    fsGroup: 1000
    runAsUser: 1000
  restartPolicy: Never
{{- end }}
