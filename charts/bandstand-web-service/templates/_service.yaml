{{- define "bandstand-web-service.service" -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
spec:
  selector: {{- include "bandstand-web-service.selectorLabels" . | nindent 4 }}
  ports:
    - port: 80
      targetPort: 8080
{{- end }}
