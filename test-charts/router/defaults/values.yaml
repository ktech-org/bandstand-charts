bandstand-router:
  ingress:
    enabled: true
    visibility: private
    subdomain: my-router
    domain: ktech.com
    defaultBackend: some-other-service

  global:
    env: test
    releaseTags:
      backstageComponent: my-component
      backstageOwner: my-team
