bandstand-web-service:
  owner: ktech-developer-platform
  env: test
  business: ktech

  ingress:
    enabled: false

  secrets:
    - name: my-first-secret
      secret: my-secret-name
      prefix: my-prefix

  test:
    resources:
      requests:
        memory: 512Mi
